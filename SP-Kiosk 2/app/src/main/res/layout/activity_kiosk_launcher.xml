<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_background"
    tools:context=".ui.kiosk.KioskLauncherActivity">

    <!-- Wallpaper Layer -->
    <ImageView
        android:id="@+id/wallpaperView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:alpha="0.3"
        tools:ignore="ContentDescription" />

    <!-- Content Layer -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Logo Placeholder (Text for now, or use actual logo resource if available) -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Screen Pulse"
            android:textAppearance="@style/TextAppearance.TV.Display"
            android:textColor="@color/primary_action"
            android:layout_marginBottom="@dimen/item_spacing" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Loading your menu screens..."
            android:textAppearance="@style/TextAppearance.TV.Subtitle"
            android:layout_marginBottom="@dimen/section_spacing" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminateTint="@color/secondary_accent" />

    </LinearLayout>

    <!-- Setup Trigger (Hidden/Subtle) -->
    <Button
        android:id="@+id/setupButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="@dimen/screen_margin"
        android:text="Tap here or 5 times to Setup"
        android:textColor="@color/text_secondary"
        android:background="@android:color/transparent"
        android:visibility="gone"
        tools:visibility="visible" />

</FrameLayout>
